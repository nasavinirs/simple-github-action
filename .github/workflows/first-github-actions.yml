name: First Simple Shell Commands
on: [push]

jobs:
  run-shell-command:
    runs-on: ubuntu-latest
    steps:
      - name: Echo a String
        run: echo "Hello, Security Gurus! This is our First GitHub Action Workflow file"